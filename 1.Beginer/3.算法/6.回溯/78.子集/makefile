Test 01:
	clang++ -std=c++11 main.cpp -o main
	./main
	rm -f main *.o

bt:
	clang++ -std=c++11 backtrack.cpp -o main
	./main
	rm -f main *.o

dfs:
	clang++ -std=c++11 dfs.cpp -o main
	./main
	rm -f main *.o


clean:
	rm -f main *.o